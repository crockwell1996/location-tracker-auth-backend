/*
const mongoose = require("mongoose");

const Location = mongoose.model(
    "Location",
    new mongoose.Schema({
        longitude: Number,
        latitude: Number,
    })
);

module.exports = Location;
*/
